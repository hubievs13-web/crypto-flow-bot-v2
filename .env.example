# crypto-flow-bot-v2 live deployment variables.
# Keep real secrets out of git.
# Telegram is enabled by default in config/code, but live startup requires both token and chat id.

CONFIG_PATH=config.yaml

# Live runner. Set false only when you intentionally want config validation without the live loop.
LIVE_RUNNER_ENABLED=true
LIVE_RUNNER_INTERVAL_SECONDS=900
LIVE_RUNNER_MAX_CYCLES=

# Legacy interval variable accepted by older deployments. Prefer LIVE_RUNNER_INTERVAL_SECONDS.
LIVE_CYCLE_INTERVAL_SECONDS=

# Telegram alerts. Default config reads TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID.
# Without TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID, live startup fails fast.
# TELEGRAM_CHAT_ID may contain one chat ID or a comma-separated list.
# To use TELEGRAM_CHAT_IDS instead, set telegram.chat_id_env: TELEGRAM_CHAT_IDS in config.yaml.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_CHAT_IDS=

# Virtual position state. Relative paths resolve from the application working directory.
POSITION_STATE_PATH=data/positions.json
